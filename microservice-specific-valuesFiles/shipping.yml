appName: shipping
configData:
  CART_ENDPOINT: "cart:8080"
  DB_HOST: "mysql-dev.chanakyakolli.online"
  DB_TYPE: "mysql"
  APP_GIT_URL: "https://github.com/roboshop-devops-project-v3/shipping"
  DB_USER: "root"
  DB_PASS: "RoboShop@1"

job:
  enabled: true
  containers:
    - name: app-user
      fileName: db/app-user.sql
    - name: master-data
      fileName: db/master-data.sql
    - name: schema
      fileName: db/schema.sql